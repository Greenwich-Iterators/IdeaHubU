import express, { Request, Response } from "express";

const departmentRouter = express.Router();

departmentRouter.post("/add", async (req: Request, res: Response) => {
	// try{
	// 	const { name}
	// }
})

export default departmentRouter