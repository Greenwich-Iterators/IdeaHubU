Install node and php

The Backend runs on `http://localhost port 9000`
The front end runs on `https://localhost port 3000`

Run the command `npm run both`

GLHF TEAM!

Guzzle PHP for uploading documents in the future.
https://docs.uploadthing.com/api-reference/openapi-spec
